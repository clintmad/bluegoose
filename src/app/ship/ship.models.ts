export class Ship {
    id: string;
    name: string;
    nickname: string;
    crew: number;

    constructor(
        id: string,
        name: string,
        nickname: string,
        crew: number
    ) {

    }
}